{
  "name": "arcoa-link-hub",
  "private": true,
  "version": "0.1.0",
  "description": "ARCOA Archive of Calm link hub",
  "scripts": {
    "build": "node scripts/check-static.mjs",
    "lint": "node --check src/data.js && node --check src/app.js",
    "dev": "live-server --host=127.0.0.1 --port=4173 --open=index.html --watch=.",
    "deploy": "git push origin main"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
